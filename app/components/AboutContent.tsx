export default function AboutContent() {
  return (
    <section id="about" className="mx-auto max-w-6xl px-4 sm:px-6 py-12 md:py-16">
      <header className="text-center mb-8">
        <h2 className="text-2xl sm:text-3xl font-extrabold tracking-tight">About InnstaStay</h2>
        <p className="mt-3 text-sm sm:text-base text-gray-600 max-w-2xl mx-auto">
          InnstaStay shows official hotel prices and policies—then sends you to the hotel to book.
          No reselling, no platform fees. Your reservation is managed by the hotel, so changes and
          questions are fast.
        </p>
      </header>
    </section>
  );
}
